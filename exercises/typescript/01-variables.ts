// Variables: values are stored using `const` (fixed, cannot be reassigned)
// or `let` (reassignable). Modify the existing values below and observe
// how the output changes.

// Command to run this exercise: npm run typescript:variables

const testerName = 'Alex';
let ticketsClosed = 0;

console.log(`${testerName} has closed ${ticketsClosed} ticket(s) today.`);

// TODO: `ticketsClosed` was declared with `let` because it's meant to change.
// Update it below (try `ticketsClosed += 3`), then add a
// second console.log using the same message format so you can see the new count.
